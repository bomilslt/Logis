web: python railway_migrate.py && python -c "from app import create_app, db; app = create_app('production'); app.app_context().push(); db.create_all()" && gunicorn --worker-class eventlet -w 1 --bind 0.0.0.0:$PORT run:app
