web: python railway_migrate.py && python -c "from app import create_app, db; app = create_app('production'); app.app_context().push(); db.create_all()" && python seed.py && gunicorn --worker-class gevent -w 1 --bind 0.0.0.0:$PORT run:app
